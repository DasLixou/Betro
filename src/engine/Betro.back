class static Betro {
    
}

implement Betro {
    func CreateWindow(title: string, width: i32, height: i32) static -> Window {
        //let window = Window::new(title, width, height); // this is currently not possible in compilation - throws error
    }
}