class Window {
    let mut title: string;
    let width: i32;
    let height: i32;
    let mut targetFPS: i32 = 60;
}

implement Window {
    static func new(title: string, width: i32, height: i32) {
        this.title = title;
        this.width = width;
        this.height = height;
    }

    func spawn() {
        
    }
}